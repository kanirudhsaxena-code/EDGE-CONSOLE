import { neon } from '@neondatabase/serverless';
import { actorMetadata, isAccessIdentityEnforced, resolveAccessActor, type AccessIdentityEnv } from './access-identity';
import { actorIdFromEvidenceRows, effectiveRunReleasePolicy } from './run-isolation';

type Env = AccessIdentityEnv & { ASSETS: Fetcher; EVIDENCE_BUCKET: R2Bucket; DATABASE_URL?: string; APP_ENV: string; OUTPUT_CONTRACT_VERSION: string; };
type JsonRecord = Record<string, unknown>;
const MAX_EVIDENCE_FILES=20,MAX_EVIDENCE_FILE_BYTES=10*1024*1024;
const ALLOWED_EVIDENCE_TYPES=new Set(['image/jpeg','image/png','image/webp','application/pdf']);
const REQUIRED_5DR_EVIDENCE_CATEGORIES=['PRICE_TECHNICALS','DERIVATIVES_OI','MARKET_TRUST','EVENT_SHOCK','EXECUTION_RISK'] as const;
const json=(data:unknown,status=200)=>new Response(JSON.stringify(data,null,2),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'private, no-store'}});
const isObject=(value:unknown):value is JsonRecord=>typeof value==='object'&&value!==null&&!Array.isArray(value);
const isNonEmptyString=(value:unknown):value is string=>typeof value==='string'&&value.trim().length>0;
const sanitizeFilename=(name:string)=>name.replace(/[^a-zA-Z0-9._-]+/g,'-').replace(/-+/g,'-').slice(0,120)||'evidence';
const evidencePrefix=(engine:string,now:Date)=>`${engine.toLowerCase()}/${now.getUTCFullYear()}/${String(now.getUTCMonth()+1).padStart(2,'0')}/${String(now.getUTCDate()).padStart(2,'0')}`;
export const FIVE_DR_DIRECTIONAL_LABELS=['STRONG_BULL','BULL','MILD_BULL','RANGE','MILD_BEAR','BEAR','STRONG_BEAR'] as const;
export function validate5drResult(result:unknown,requireReleaseCompleteness=false):string[]{
  const errors:string[]=[];
  if(!isObject(result))return['result must be an object'];
  if(result.model_version!=='5DR_V2_1')errors.push('result.model_version must be 5DR_V2_1');
  if(result.output_contract_version!=='5DR_V2_1_2')errors.push('result.output_contract_version must be 5DR_V2_1_2');

  const requiredHorizons=['D+1','D+2','D+3','D+4','D+5'];
  const horizons=result.horizon_slots;
  if(!isObject(horizons))errors.push('result.horizon_slots must be an object');
  else for(const horizon of requiredHorizons){
    const slot=horizons[horizon];
    if(!isObject(slot)){errors.push(\`result.horizon_slots.\${horizon} must be a complete object\`);continue}
    const direction=String(slot.direction??'');
    if(!['BULLISH','RANGE','BEARISH'].includes(direction))errors.push(\`result.horizon_slots.\${horizon}.direction is invalid\`);
    const probs=slot.probabilities;
    if(!isObject(probs))errors.push(\`result.horizon_slots.\${horizon}.probabilities must contain BULL/RANGE/BEAR\`);
    else{
      const keys=['BULL','RANGE','BEAR'];
      for(const key of keys)if(typeof probs[key]!=='number'||!Number.isFinite(probs[key] as number)||(probs[key] as number)<0||(probs[key] as number)>100)errors.push(\`result.horizon_slots.\${horizon}.probabilities.\${key} must be 0-100\`);
      if(keys.every(key=>typeof probs[key]==='number'&&Number.isFinite(probs[key] as number))){
        const sum=keys.reduce((acc,key)=>acc+Number(probs[key]),0);
        if(Math.abs(sum-100)>0.02)errors.push(\`result.horizon_slots.\${horizon}.probabilities must sum to 100\`);
        const selected=direction==='BULLISH'?'BULL':direction==='BEARISH'?'BEAR':'RANGE';
        const max=Math.max(...keys.map(key=>Number(probs[key])));
        if(Math.abs(Number(probs[selected])-max)>0.02)errors.push(\`result.horizon_slots.\${horizon}.direction must match highest scenario probability\`);
      }
    }
    if(typeof slot.zone_low!=='number'||!Number.isFinite(slot.zone_low as number)||(slot.zone_low as number)<=0)errors.push(\`result.horizon_slots.\${horizon}.zone_low must be positive\`);
    if(typeof slot.zone_high!=='number'||!Number.isFinite(slot.zone_high as number)||(slot.zone_high as number)<=0)errors.push(\`result.horizon_slots.\${horizon}.zone_high must be positive\`);
    if(typeof slot.zone_low==='number'&&typeof slot.zone_high==='number'&&slot.zone_high<slot.zone_low)errors.push(\`result.horizon_slots.\${horizon}.zone_high must be >= zone_low\`);
    if(!isNonEmptyString(slot.basis))errors.push(\`result.horizon_slots.\${horizon}.basis is mandatory\`);
  }

  if(typeof result.des5!=='number'||!Number.isFinite(result.des5))errors.push('result.des5 must be a finite number');
  if(!FIVE_DR_DIRECTIONAL_LABELS.includes(String(result.directional_label) as typeof FIVE_DR_DIRECTIONAL_LABELS[number]))errors.push('result.directional_label must match the canonical 5DR directional label set');
  if(typeof result.market_trust!=='number'||!Number.isFinite(result.market_trust))errors.push('result.market_trust must be a finite number');
  if(!isNonEmptyString(result.market_trust_band))errors.push('result.market_trust_band is mandatory');

  if(!isObject(result.probabilities))errors.push('result.probabilities must be an object');
  else{
    const keys=['BULL','RANGE','BEAR'];
    for(const label of keys)if(typeof result.probabilities[label]!=='number'||!Number.isFinite(result.probabilities[label] as number))errors.push(\`result.probabilities.\${label} must be a finite number\`);
    if(keys.every(key=>typeof result.probabilities[key]==='number'&&Number.isFinite(result.probabilities[key] as number))){
      const sum=keys.reduce((acc,key)=>acc+Number(result.probabilities[key]),0);
      if(Math.abs(sum-100)>0.02)errors.push('result.probabilities must sum to 100');
    }
  }

  if(typeof result.execution_edge!=='number'||!Number.isFinite(result.execution_edge))errors.push('result.execution_edge must be a finite number');
  if(typeof result.tradeable!=='boolean')errors.push('result.tradeable must be boolean');
  if(!Array.isArray(result.tradeability_blockers))errors.push('result.tradeability_blockers must be an array');
  if(!['BUY_CE','BUY_PE','BUY_CONVEXITY','NO_TRADE'].includes(String(result.recommendation)))errors.push('result.recommendation must use frozen 5DR vocabulary');
  if(!isObject(result.tradeability_gate))errors.push('result.tradeability_gate is mandatory');
  if(!isObject(result.engine_diagnostics))errors.push('result.engine_diagnostics is mandatory');
  if(!isNonEmptyString(result.forecast_assessment))errors.push('result.forecast_assessment is mandatory');
  if(!isNonEmptyString(result.recommendation_assessment))errors.push('result.recommendation_assessment is mandatory');

  if(requireReleaseCompleteness){
    if(result.assessment_snapshot_complete!==true)errors.push('result.assessment_snapshot_complete must be true for published V2.1.2 runs');
    if(result.recommendation_ledger_complete!==true)errors.push('result.recommendation_ledger_complete must be true for published V2.1.2 runs');
    if(!isObject(result.assessment_snapshot))errors.push('result.assessment_snapshot must be persisted for published V2.1.2 runs');
    else{
      const metrics=result.assessment_snapshot.metrics;
      if(!isObject(metrics))errors.push('result.assessment_snapshot.metrics is mandatory');
      else{
        const day=metrics.day_metrics;
        if(!isObject(day)||!['D','D+1','D+2','D+3','D+4'].every(label=>isObject(day[label])))errors.push('result.assessment_snapshot must contain all five assessment horizons');
        const ledger=metrics.recommendation_ledger;
        if(!Array.isArray(ledger))errors.push('result.assessment_snapshot recommendation ledger is mandatory');
        else{
          const expected=Number(metrics.all_recommendations_count??ledger.length);
          if(ledger.length!==expected)errors.push('result.assessment_snapshot recommendation ledger count mismatch');
        }
      }
    }
  }
  return errors;
}
function validate5drEnvelope(body:unknown):string[]{const errors:string[]=[];if(!isObject(body))return['request body must be a JSON object'];if(body.contract_version!=='1.0')errors.push('contract_version must be 1.0');if(body.engine!=='5DR')errors.push('engine must be 5DR');if(!isNonEmptyString(body.run_id))errors.push('run_id is mandatory');if(!isNonEmptyString(body.framework_version))errors.push('framework_version is mandatory');if(!['SUCCESS','PARTIAL','FAILED','SHADOW'].includes(String(body.status)))errors.push('status must be SUCCESS, PARTIAL, FAILED or SHADOW');if(!isNonEmptyString(body.generated_at)||Number.isNaN(Date.parse(body.generated_at)))errors.push('generated_at must be a valid ISO timestamp');const provenance=body.provenance;if(!isObject(provenance))errors.push('provenance is mandatory');else{if(!['MANUAL','HYBRID','AUTOMATED'].includes(String(provenance.mode)))errors.push('provenance.mode must be MANUAL, HYBRID or AUTOMATED');if(!Array.isArray(provenance.sources))errors.push('provenance.sources must be an array')}errors.push(...validate5drResult(body.result,body.published===true));return errors}
async function uploadEvidence(request:Request,env:Env):Promise<Response>{if(!env.DATABASE_URL)return json({error:'Database is not configured'},503);if(!env.EVIDENCE_BUCKET)return json({error:'Evidence storage is not configured'},503);let form:FormData;try{form=await request.formData()}catch{return json({error:'Invalid multipart form data'},400)}const engine=String(form.get('engine')||'').trim(),provenanceMode=String(form.get('provenance_mode')||'').trim().toUpperCase(),capturedAtRaw=String(form.get('captured_at')||'').trim(),files=form.getAll('files').filter((entry):entry is File=>entry instanceof File);if(engine!=='5DR')return json({error:'Evidence upload currently supports 5DR only'},422);if(!['MANUAL','HYBRID','AUTOMATED'].includes(provenanceMode))return json({error:'provenance_mode must be MANUAL, HYBRID or AUTOMATED'},422);if(!files.length)return json({error:'At least one evidence file is required'},422);if(files.length>MAX_EVIDENCE_FILES)return json({error:`Maximum ${MAX_EVIDENCE_FILES} evidence files per upload`},413);for(const file of files){if(!ALLOWED_EVIDENCE_TYPES.has(file.type))return json({error:`Unsupported evidence type: ${file.type||'unknown'}`,file:file.name},415);if(file.size<=0||file.size>MAX_EVIDENCE_FILE_BYTES)return json({error:'Each evidence file must be between 1 byte and 10 MB',file:file.name},413)}const capturedAt=capturedAtRaw&&!Number.isNaN(Date.parse(capturedAtRaw))?new Date(capturedAtRaw).toISOString():new Date().toISOString(),now=new Date(),uploadBatchId=crypto.randomUUID(),sql=neon(env.DATABASE_URL),uploaded:Array<Record<string,unknown>>=[],storedKeys:string[]=[];try{for(const file of files){const uploadId=crypto.randomUUID(),filename=sanitizeFilename(file.name),objectKey=`${evidencePrefix(engine,now)}/${uploadBatchId}/${uploadId}-${filename}`;await env.EVIDENCE_BUCKET.put(objectKey,file.stream(),{httpMetadata:{contentType:file.type},customMetadata:{engine,provenance_mode:provenanceMode,upload_id:uploadId,batch_id:uploadBatchId,original_filename:filename,captured_at:capturedAt}});storedKeys.push(objectKey);await sql`insert into evidence_uploads (upload_id,batch_id,engine,provenance_mode,object_key,file_name,mime_type,size_bytes,captured_at,status,metadata) values (${uploadId},${uploadBatchId},${engine},${provenanceMode},${objectKey},${filename},${file.type},${file.size},${capturedAt},'STAGED',${JSON.stringify({storage:'R2',bucket_binding:'EVIDENCE_BUCKET'})}::jsonb)`;uploaded.push({upload_id:uploadId,file_name:filename,mime_type:file.type,size_bytes:file.size,captured_at:capturedAt,status:'STAGED'})}}catch(error){console.error('Evidence upload failed',error);await Promise.allSettled(storedKeys.map(key=>env.EVIDENCE_BUCKET.delete(key)));if(uploaded.length)await sql`delete from evidence_uploads where batch_id=${uploadBatchId}`;return json({error:'Evidence upload failed; staged files were rolled back'},500)}return json({ok:true,batch_id:uploadBatchId,engine,provenance_mode:provenanceMode,file_count:uploaded.length,evidence:uploaded,next_step:'Apply 5DR evidence readiness gate'},201)}
async function create5drRunRequest(request:Request,env:Env):Promise<Response>{if(!env.DATABASE_URL)return json({error:'Database is not configured'},503);const actor=await resolveAccessActor(request,env);if(isAccessIdentityEnforced(env)&&!actor.authenticated)return json({error:'Authenticated Console identity is required'},401);let body:unknown;try{body=await request.json()}catch{return json({error:'Invalid JSON body'},400)}if(!isObject(body)||!isNonEmptyString(body.batch_id))return json({error:'batch_id is mandatory'},422);const assessment=isObject(body.assessment)?body.assessment:{};const allowedObjectives=['MARKET_VIEW','OPTIONS_SETUP','BOTH'],allowedRisk=['CONSERVATIVE','BALANCED','OPPORTUNISTIC'],allowedPriority=['CAPITAL_PROTECTION','BALANCED','GROWTH'];const objective=String(assessment.objective??'BOTH'),risk_posture=String(assessment.risk_posture??'CONSERVATIVE'),capital_priority=String(assessment.capital_priority??'CAPITAL_PROTECTION');if(!allowedObjectives.includes(objective))return json({error:'Unknown decision objective'},422);if(!allowedRisk.includes(risk_posture))return json({error:'Unknown risk posture'},422);if(!allowedPriority.includes(capital_priority))return json({error:'Unknown capital priority'},422);const decisionSetup={horizon:'D+1_TO_D+5',objective,risk_posture,capital_priority,assessed_at:new Date().toISOString()};const categories=Array.isArray(body.evidence_categories)?[...new Set(body.evidence_categories.map(String))]:[],invalid=categories.filter(category=>!REQUIRED_5DR_EVIDENCE_CATEGORIES.includes(category as typeof REQUIRED_5DR_EVIDENCE_CATEGORIES[number])),missing=REQUIRED_5DR_EVIDENCE_CATEGORIES.filter(category=>!categories.includes(category));if(invalid.length)return json({error:'Unknown 5DR evidence categories',invalid_categories:invalid},422);if(missing.length)return json({error:'5DR evidence readiness gate blocked',missing_categories:missing,required_categories:REQUIRED_5DR_EVIDENCE_CATEGORIES},409);const batchId=String(body.batch_id),sql=neon(env.DATABASE_URL),existing=await sql`select request_id,engine,batch_id,provenance_mode,framework_version,output_contract_version,status,run_id,metadata,created_at,updated_at from analysis_requests where batch_id=${batchId} limit 1`;if(existing.length)return json({ok:true,request:existing[0],idempotent:true});const evidence=await sql`select upload_id,batch_id,engine,provenance_mode,mime_type,captured_at,status,metadata from evidence_uploads where batch_id=${batchId} order by id`;if(!evidence.length)return json({error:'Evidence batch not found'},404);if(evidence.some(row=>row.engine!=='5DR'))return json({error:'Evidence batch is not a 5DR batch'},422);if(evidence.some(row=>row.status!=='STAGED'))return json({error:'Evidence batch is not fully staged'},409);const provenanceMode=String(evidence[0].provenance_mode);if(evidence.some(row=>row.provenance_mode!==provenanceMode))return json({error:'Evidence batch has mixed provenance modes'},422);const evidenceActorId=actorIdFromEvidenceRows(evidence as Array<{metadata?:unknown}>);if(isAccessIdentityEnforced(env)&&actor.role!=='OWNER'&&evidenceActorId!==actor.id)return json({error:'Evidence batch belongs to a different Console user'},403);const requestId=`5drreq_${crypto.randomUUID()}`,capturedTimes=evidence.map(row=>row.captured_at).filter(Boolean).map(value=>new Date(String(value)).getTime()).filter(value=>!Number.isNaN(value)),freshnessAt=capturedTimes.length?new Date(Math.max(...capturedTimes)).toISOString():null,metadata={actor:actorMetadata(actor),identity_enforced:isAccessIdentityEnforced(env),sandbox_requested:false,decision_setup:decisionSetup,evidence_file_count:evidence.length,evidence_mime_types:[...new Set(evidence.map(row=>String(row.mime_type)))],freshness_at:freshnessAt,evidence_readiness:{status:'EVIDENCE_READY',declared_categories:categories,required_categories:REQUIRED_5DR_EVIDENCE_CATEGORIES,assessed_at:new Date().toISOString(),basis:'USER_DECLARATION'},adapter_stage:'EVIDENCE_READY'};await sql`insert into analysis_requests (request_id,engine,batch_id,provenance_mode,framework_version,output_contract_version,status,metadata) values (${requestId},'5DR',${batchId},${provenanceMode},'5DR_V2_1','5DR_V2_1_2','READY_FOR_ENGINE',${JSON.stringify(metadata)}::jsonb)`;await sql`update evidence_uploads set request_id=${requestId},status='READY_FOR_ENGINE' where batch_id=${batchId}`;return json({ok:true,request:{request_id:requestId,engine:'5DR',batch_id:batchId,provenance_mode:provenanceMode,framework_version:'5DR_V2_1',output_contract_version:'5DR_V2_1_2',status:'READY_FOR_ENGINE',metadata},next_step:'Governed normalization'},201)}
async function api(request:Request,env:Env):Promise<Response>{const url=new URL(request.url);if(url.pathname==='/api/health')return json({ok:true,service:'EDGE Console',environment:env.APP_ENV,contract_version:env.OUTPUT_CONTRACT_VERSION,database_configured:Boolean(env.DATABASE_URL),evidence_storage_configured:Boolean(env.EVIDENCE_BUCKET),integrations:{'5DR':'V2.1.2','5DR_EVIDENCE_READINESS':'READY','5DR_RUN_REQUEST_ADAPTER':'READY'}});if(url.pathname==='/api/evidence/upload'&&request.method==='POST')return uploadEvidence(request,env);if(url.pathname==='/api/5dr/run-requests'&&request.method==='POST')return create5drRunRequest(request,env);if(url.pathname==='/api/5dr/run-requests/latest'&&request.method==='GET'){if(!env.DATABASE_URL)return json({request:null,note:'DATABASE_URL not configured yet'},503);const sql=neon(env.DATABASE_URL),rows=await sql`select request_id,engine,batch_id,provenance_mode,framework_version,output_contract_version,status,run_id,metadata,error,created_at,updated_at from analysis_requests where engine='5DR' order by created_at desc limit 1`;return json({request:rows[0]??null,note:rows.length?undefined:'No 5DR run request yet'})}if(url.pathname==='/api/5dr/run-request'&&request.method==='GET'){if(!env.DATABASE_URL)return json({request:null,note:'DATABASE_URL not configured yet'},503);const runId=url.searchParams.get('run_id');if(!isNonEmptyString(runId))return json({request:null,error:'run_id is mandatory'},422);const sql=neon(env.DATABASE_URL),rows=await sql`select request_id,engine,batch_id,provenance_mode,framework_version,output_contract_version,status,run_id,metadata,error,created_at,updated_at from analysis_requests where engine='5DR' and run_id=${runId} order by updated_at desc limit 1`;return json({request:rows[0]??null,note:rows.length?undefined:'No request found for this published run'})}if(url.pathname==='/api/engines'){if(!env.DATABASE_URL)return json({engines:[{id:'5DR',name:'5DR',mode:'HYBRID',status:'EVIDENCE_GATE_READY',version:'2.1.2'},{id:'EDGE_STOCKS',name:'EDGE Stocks',mode:'HYBRID',status:'FOUNDATION',version:'1.0'},{id:'EDGE_IPO',name:'EDGE IPO',mode:'AUTOMATED',status:'INTEGRATION_PENDING',version:'1.0'}]});const sql=neon(env.DATABASE_URL),rows=await sql`select engine as id,display_name as name,production_version as version,status from engine_registry order by case engine when '5DR' then 1 when 'EDGE_STOCKS' then 2 else 3 end`;return json({engines:rows.map(row=>({...row,mode:row.id==='EDGE_IPO'?'AUTOMATED':'HYBRID',status:row.id==='5DR'?'EVIDENCE_GATE_READY':row.status}))})}if(url.pathname==='/api/runs/latest'&&request.method==='GET'){if(!env.DATABASE_URL)return json({runs:[],note:'DATABASE_URL not configured yet'});const sql=neon(env.DATABASE_URL),engine=url.searchParams.get('engine'),runs=engine?await sql`select run_id,engine,contract_version,framework_version,status,provenance_mode,freshness_at,generated_at,published,result,warnings from analysis_runs where published=true and engine=${engine} order by generated_at desc limit 20`:await sql`select run_id,engine,contract_version,framework_version,status,provenance_mode,freshness_at,generated_at,published from analysis_runs where published=true order by generated_at desc limit 20`;return json({runs})}if(url.pathname==='/api/5dr/latest'&&request.method==='GET'){if(!env.DATABASE_URL)return json({run:null,note:'DATABASE_URL not configured yet'},503);const sql=neon(env.DATABASE_URL),rows=await sql`select run_id,contract_version,framework_version,status,provenance_mode,sources,freshness_at,generated_at,result,warnings from analysis_runs where engine='5DR' and published=true order by generated_at desc limit 1`;return json({run:rows[0]??null,note:rows.length?undefined:'No published 5DR run yet'})}if(url.pathname==='/api/5dr/outcome-assessment'&&request.method==='GET'){if(!env.DATABASE_URL)return json({assessment:null,note:'DATABASE_URL not configured yet'},503);const runId=url.searchParams.get('run_id');if(!isNonEmptyString(runId))return json({assessment:null,error:'run_id is mandatory'},422);const sql=neon(env.DATABASE_URL),rows=await sql`select run_id,assessment_horizon,outcome,score,metrics,assessed_at from outcome_assessments where run_id=${runId} order by assessed_at desc limit 1`;return json({assessment:rows[0]??null,note:rows.length?undefined:'Performance assessment pending'})}if(url.pathname==='/api/assessment-import'&&request.method==='POST'){if(!env.DATABASE_URL)return json({error:'Database is not configured'},503);let body:unknown;try{body=await request.json()}catch{return json({error:'Invalid JSON body'},400)}if(!isObject(body)||String(body.engine||'')!=='5DR'||!isNonEmptyString(body.forecast_id)||!isNonEmptyString(body.assessed_at)||!isObject(body.metrics))return json({error:'Invalid assessment import payload'},422);const sql=neon(env.DATABASE_URL),sourceId=String(body.forecast_id),existing=await sql`select id from assessment_rollups where engine='5DR' and source_id=${sourceId} and assessed_at=${String(body.assessed_at)}::timestamptz limit 1`;if(existing.length)return json({ok:true,duplicate:true,source_id:sourceId});await sql`insert into assessment_rollups (engine,source_id,assessed_at,headline,score,metrics) values ('5DR',${sourceId},${String(body.assessed_at)}::timestamptz,${isNonEmptyString(body.outcome)?String(body.outcome):null},${typeof body.score==='number'?body.score:null},${JSON.stringify(body.metrics)}::jsonb)`;return json({ok:true,source_id:sourceId},201)}
if(url.pathname==='/api/assessment-summary'&&request.method==='GET'){if(!env.DATABASE_URL)return json({summary:null,note:'DATABASE_URL not configured yet'},503);const engine=url.searchParams.get('engine')||'5DR',sql=neon(env.DATABASE_URL);if(engine==='5DR'){const imported=await sql`select source_id,headline,score,metrics,assessed_at from assessment_rollups where engine='5DR' order by created_at desc,id desc limit 1`;if(imported.length){
const row:any=imported[0],m=isObject(row.metrics)?row.metrics:{},canonical=isObject(m.canonical_selection)?m.canonical_selection:null,overall=isObject(m.overall_forecast_metrics)?m.overall_forecast_metrics:{},day=isObject(m.day_metrics)?m.day_metrics:{},dayRec=isObject(m.day_recommendation_metrics)?m.day_recommendation_metrics:{},rec=isObject(m.recommendation_metrics)?m.recommendation_metrics:{},returns=isObject(m.return_metrics)?m.return_metrics:{};
const forecastTotal=Number(overall.canonical_scorable_checkpoints??overall.canonical_scorable??0),forecastEligible=Number(overall.canonical_matured_eligible_checkpoints??forecastTotal),forecastMissing=Number(overall.missing_unscorable_checkpoints??Math.max(forecastEligible-forecastTotal,0)),forecastCoverage=typeof overall.scorable_coverage_pct==='number'?overall.scorable_coverage_pct:(forecastEligible?Number((forecastTotal/forecastEligible*100).toFixed(2)):null),forecastHits=Number(overall.directional_hits??0),recTotal=Number(rec.resolved??0),recHits=Number(rec.wins??0);
const pendingRows=await sql`
with legacy_ranked as (
  select ar.run_id,ar.generated_at,ar.freshness_at,ar.result,
         row_number() over (
           partition by (ar.generated_at at time zone 'Asia/Kolkata')::date
           order by ar.generated_at desc,ar.run_id desc
         ) as legacy_rank
    from analysis_runs ar
   where ar.engine='5DR'
     and ar.published=true
     and ar.status='SUCCESS'
     and ar.generated_at < timestamptz '2026-09-21 18:30:00+00'
     and not exists (
       select 1 from outcome_assessments oa where oa.run_id=ar.run_id
     )
)
select run_id,generated_at,freshness_at,result
  from legacy_ranked
 where legacy_rank=1
 order by generated_at desc
 limit 12`;
const pending=pendingRows.map((p:any)=>{const result=isObject(p.result)?p.result:{},probs=isObject(result.probabilities)?result.probabilities:{},slots=isObject(result.horizon_slots)?result.horizon_slots:{};return{run_id:p.run_id,generated_at:p.generated_at,freshness_at:p.freshness_at,directional_label:result.directional_label??null,probabilities:probs,horizon_slots:slots,tradeable:result.tradeable===true,market_trust:typeof result.market_trust==='number'?result.market_trust:null,execution_edge:typeof result.execution_edge==='number'?result.execution_edge:null}});
return json({summary:{engine:'5DR',canonical, population_rule:String(overall.population_rule??rec.population_rule??'SELECTED_DAILY_CANONICAL_ONLY'),matured_runs:forecastEligible,matured_eligible_checkpoints:forecastEligible,scorable_checkpoints:forecastTotal,missing_unscorable_checkpoints:forecastMissing,scorable_coverage_pct:forecastCoverage,pending_forecasts:pending.length,assessment_as_of:row.assessed_at,forecast:{population:'SCORABLE_MATURED_CANONICAL_CHECKPOINTS',eligible_total:forecastEligible,total:forecastTotal,missing_unscorable:forecastMissing,coverage_pct:forecastCoverage,hits:forecastHits,accuracy_pct:typeof overall.directional_accuracy_pct==='number'?overall.directional_accuracy_pct:(forecastTotal?Number((forecastHits/forecastTotal*100).toFixed(2)):null)},recommendation:{population:'RESOLVED_CANONICAL_ACTIONABLE_RECOMMENDATIONS',total:recTotal,hits:recHits,accuracy_pct:typeof rec.hit_rate_pct==='number'?rec.hit_rate_pct:(recTotal?Number((recHits/recTotal*100).toFixed(2)):null),no_trade_calls:Number(rec.no_trade_calls??0)},returns:{population:'CANONICAL_ACTIONABLE_RECOMMENDATIONS',absolute_return_pct:typeof returns.cumulative_resolved_pnl_pct==='number'?returns.cumulative_resolved_pnl_pct:null,hits_return_pct:typeof returns.hit_pnl_pct==='number'?returns.hit_pnl_pct:null,misses_return_pct:typeof returns.miss_pnl_pct==='number'?returns.miss_pnl_pct:null}},pending_forecasts:pending,details:[{run_id:row.source_id,assessment_horizon:'TILL_DATE',outcome:row.headline,score:row.score,metrics:{canonical_selection:canonical,day_wise:day,zone_wise:Object.fromEntries(Object.entries(day).map(([k,v]:any)=>[k,isObject(v)?{zone_hits:v.zone_hits,scorable:v.scorable,eligible_matured:v.eligible_matured,missing_unscorable:v.missing_unscorable,coverage_pct:v.coverage_pct,zone_hit_rate_pct:v.zone_hit_rate_pct}:v])),recommendation_metrics:rec,day_recommendation_metrics:dayRec,return_metrics:returns},assessed_at:row.assessed_at}]})
}}const rows=await sql`select oa.run_id,oa.assessment_horizon,oa.outcome,oa.score,oa.metrics,oa.assessed_at,ar.generated_at from outcome_assessments oa join analysis_runs ar on ar.run_id=oa.run_id where ar.engine=${engine} and ar.published=true order by oa.assessed_at asc`;let forecastTotal=0,forecastHits=0,recommendationTotal=0,recommendationHits=0,overallReturn=0,hitReturn=0,missReturn=0,returnCount=0,hitReturnCount=0,missReturnCount=0;const details=rows.map((row:any)=>{const m=isObject(row.metrics)?row.metrics:{};const fh=typeof m.forecast_hit==='boolean'?m.forecast_hit:null,rh=typeof m.recommendation_hit==='boolean'?m.recommendation_hit:null;const ret=typeof m.absolute_return_pct==='number'?m.absolute_return_pct:(typeof m.return_pct==='number'?m.return_pct:null);if(fh!==null){forecastTotal++;if(fh)forecastHits++}if(rh!==null){recommendationTotal++;if(rh)recommendationHits++}if(typeof ret==='number'&&Number.isFinite(ret)){overallReturn+=ret;returnCount++;const isHit=rh??fh;if(isHit===true){hitReturn+=ret;hitReturnCount++}else if(isHit===false){missReturn+=ret;missReturnCount++}}return {run_id:row.run_id,assessment_horizon:row.assessment_horizon,outcome:row.outcome,score:row.score,metrics:m,assessed_at:row.assessed_at,generated_at:row.generated_at}});return json({summary:{engine,matured_runs:rows.length,forecast:{total:forecastTotal,hits:forecastHits,accuracy_pct:forecastTotal?Number((forecastHits/forecastTotal*100).toFixed(1)):null},recommendation:{total:recommendationTotal,hits:recommendationHits,accuracy_pct:recommendationTotal?Number((recommendationHits/recommendationTotal*100).toFixed(1)):null},returns:{absolute_return_pct:returnCount?Number(overallReturn.toFixed(2)):null,hits_return_pct:hitReturnCount?Number(hitReturn.toFixed(2)):null,misses_return_pct:missReturnCount?Number(missReturn.toFixed(2)):null}},details})}if(url.pathname==='/api/5dr/runs'&&request.method==='POST'){if(!env.DATABASE_URL)return json({error:'Database is not configured'},503);let body:unknown;try{body=await request.json()}catch{return json({error:'Invalid JSON body'},400)}const errors=validate5drEnvelope(body);if(errors.length)return json({error:'5DR contract validation failed',details:errors},422);const payload=body as JsonRecord,provenance=payload.provenance as JsonRecord,sql=neon(env.DATABASE_URL),existing=await sql`select run_id from analysis_runs where run_id=${String(payload.run_id)} limit 1`;if(existing.length)return json({error:'run_id already exists; runs are immutable'},409);const requestId=isNonEmptyString(payload.request_id)?String(payload.request_id):null;if(requestId){const requests=await sql`select request_id,status from analysis_requests where request_id=${requestId} and engine='5DR' limit 1`;if(!requests.length)return json({error:'request_id not found'},422);if(!['READY_FOR_ENGINE','PROCESSING'].includes(String(requests[0].status)))return json({error:'request_id is not eligible for completion'},409)}const status=String(payload.status),requestedPublish=payload.published===true;if(requestedPublish&&status!=='SUCCESS')return json({error:'Only SUCCESS runs may be published'},422);const requestRows=requestId?await sql`select metadata from analysis_requests where request_id=${requestId} and engine='5DR' limit 1`:[],requestMetadata=requestRows.length&&isObject(requestRows[0].metadata)?requestRows[0].metadata:{},releasePolicy=effectiveRunReleasePolicy(requestMetadata,requestedPublish,payload.learning_eligible!==false);const sources=Array.isArray(provenance.sources)?provenance.sources:[],freshnessAt=isNonEmptyString(provenance.freshness_at)?provenance.freshness_at:null,warnings=Array.isArray(payload.warnings)?payload.warnings:[];await sql`insert into analysis_runs (run_id,engine,contract_version,framework_version,status,provenance_mode,sources,freshness_at,generated_at,result,warnings,learning_eligible,published) values (${String(payload.run_id)},'5DR',${String(payload.contract_version)},${String(payload.framework_version)},${status},${String(provenance.mode)},${JSON.stringify(sources)}::jsonb,${freshnessAt},${String(payload.generated_at)},${JSON.stringify(payload.result)}::jsonb,${JSON.stringify(warnings)}::jsonb,${releasePolicy.learning_eligible},${releasePolicy.published})`;if(requestId){const nextMeta={...requestMetadata,adapter_stage:'COMPLETED',completion:{status:releasePolicy.completion_status,run_id:String(payload.run_id),sandbox:releasePolicy.sandbox,completed_at:new Date().toISOString()}};await sql`update analysis_requests set status='COMPLETED',run_id=${String(payload.run_id)},metadata=${JSON.stringify(nextMeta)}::jsonb,error=null,updated_at=now() where request_id=${requestId}`;await sql`update evidence_uploads set status='ATTACHED',run_id=${String(payload.run_id)} where request_id=${requestId}`}return json({ok:true,run_id:payload.run_id,engine:'5DR',published:releasePolicy.published,learning_eligible:releasePolicy.learning_eligible,sandbox:releasePolicy.sandbox,contract:'5DR_V2_1_2',request_id:requestId},201)}return json({error:'Not found'},404)}
export default{async fetch(request:Request,env:Env):Promise<Response>{const url=new URL(request.url);if(url.pathname.startsWith('/api/'))return api(request,env);const asset=await env.ASSETS.fetch(request);const headers=new Headers(asset.headers);headers.set('cache-control','no-store, max-age=0');headers.set('pragma','no-cache');headers.set('expires','0');return new Response(asset.body,{status:asset.status,statusText:asset.statusText,headers})}};
